---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
metadata:
  name: infrastructure
  namespace: flux-system

resources:
  - sealed-secrets-release.yaml # need to come first, because others depend on it
  - cilium-release.yaml
  #- network-policies
  - longhorn
  - metrics-server-release.yaml
#  - network-testing
#  - metallb-release.yaml
