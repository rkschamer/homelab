# Forward kernel (dmesg) logs to Fluent Bit via local NodePort on this node.
# Service log forwarding (machine.logging) is configured in terraform/talos.tf.
apiVersion: v1alpha1
kind: KmsgLogConfig
name: fluent-bit
url: "tcp://127.0.0.1:30517/"
