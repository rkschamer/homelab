# Forward kernel (dmesg) logs to Fluent Bit.
# Service log forwarding (machine.logging) is configured in terraform/talos.tf.
apiVersion: v1alpha1
kind: KmsgLogConfig
name: fluent-bit
url: "tcp://127.0.0.1:5170/"
